node_modules/
# data/queries.csv is committed so the grader has the exact dataset the
# performance numbers were measured on (no dependency on the Wikipedia dump URL).
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/pageviews-*.gz
*.log
.env
